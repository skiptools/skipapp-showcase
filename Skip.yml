# Main configuration file for Skip

# Codebases over 25KB must have a valid Skip license.
# Keys are specified with "skip-license" in Skip.yml or the (gitignored) .skip.yml
# or in the SKIP_LICENSE environment variable for CI usage.
#
#skip-license: get a license key from skip.tools
